# ===== 核心框架 =====
Django==4.2.11                  # Web 框架
djangorestframework==3.14.0     # RESTful API 框架
django-cors-headers==4.3.1      # 跨域支持（前后端分离必备）

# ===== 推荐算法 =====
jieba==0.42.1                   # 中文分词
scikit-learn==1.3.2             # TfidfVectorizer + cosine_similarity
numpy==1.26.4                   # 数值计算（sklearn 依赖）
scipy==1.11.4                   # 稀疏矩阵（sklearn 依赖）

# ===== 工具类 =====
python-dotenv==1.0.1            # 环境变量管理（可选）

# ===== 开发期可选 =====
# 如果需要 JWT 鉴权，取消下行注释：
# djangorestframework-simplejwt==5.3.1

# 如果简历支持上传头像/附件，取消下行注释：
# Pillow==10.2.0

# 如果需要切到 PostgreSQL，取消下行注释：
# psycopg2-binary==2.9.9

# 如果需要 Redis 缓存岗位向量，取消下行注释：
# redis==5.0.1
# django-redis==5.4.0
